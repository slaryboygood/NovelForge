# NovelForge 环境变量示例
#
# 复制为 `.env.local` 后填入真实值（`.env.local` 已被 .gitignore 忽略，不会进入版本控制）。
# 
# ⚠️ 当前版本（Product V3.0）说明：
#   所有生成（题材 / 基调 / 卖点候选、设定候选、章纲）都是**本地确定性流程**；
#   产品路径没有接入 LLM provider，因此下面这些 key **目前不会被使用**，
#   配置与不配置的行为完全一致（API 响应 source 恒为 rule，notes 含 AI_UNAVAILABLE）。
#   这里保留变量名，是为了后续版本接入模型时不需要改文档结构。
#   模型接入属于后续版本能力；接入时会同步更新 README、本文件与 UI 文案。

# 预留：模型服务 key（当前版本不生效）
DEEPSEEK_API_KEY=
ARK_API_KEY=

# 预留：覆盖默认服务地址（当前版本不生效）
# DEEPSEEK_BASE_URL=
# ARK_BASE_URL=
