# NovelForge 环境变量示例
#
# 复制为 `.env.local` 后填入真实值（`.env.local` 已被 .gitignore 忽略，不会进入版本控制）。
# 
# NovelForge V4-02 起：模型调用统一经过 src/novelforge/ai（LLM Gateway）。
#
# 重要：默认**不启用任何 provider**（novel/config/ai/providers.json 全部 enabled=false），
# 因此默认行为仍然是"不调用模型"：创意 / 设定 / 章纲等现有生成仍是本地确定性流程，
# API 响应的 source 恒为 rule、notes 含 AI_UNAVAILABLE。
#
# 要真正启用模型，请二选一：
#   1) 编辑 novel/config/ai/providers.json：把某个 provider 的 enabled 改成 true，
#      并设置它声明的 api_key_env（下面的变量名只是示例）；
#   2) 用环境变量直接注入完整配置：
#      NOVELFORGE_LLM_PROVIDERS={"providers":[{...}]}
#
# api_key 只能通过环境变量提供：不写入仓库、不写入 trace / log / cache key / API 响应。

# 示例：OpenAI-compatible 服务的 key（变量名必须与 provider 配置里的 api_key_env 一致）
DEEPSEEK_API_KEY=
ARK_API_KEY=
NOVELFORGE_LLM_API_KEY=
NOVELFORGE_LOCAL_LLM_API_KEY=

# 可选：legacy 适配器（M3 spec / M6 plot / M7 route）的模型与端点
# 这些适配器不再自带默认模型与默认端点，必须显式配置：
# NOVELFORGE_SPEC_MODEL=
# NOVELFORGE_SPEC_BASE_URL=
# NOVELFORGE_PLOT_MODEL=
# NOVELFORGE_PLOT_BASE_URL=
# NOVELFORGE_ROUTE_MODEL=
# NOVELFORGE_ROUTE_BASE_URL=

# 预留：覆盖默认服务地址（provider 配置的 base_url 优先）
# DEEPSEEK_BASE_URL=
# ARK_BASE_URL=
