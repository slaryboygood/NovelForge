# 校准规则

约每 10 章生成一次 `MiniArcCalibration`，只检查人物弧、关系、速度、爽点、谜题、伏笔、装备、能力和长期角色存在感，不自动修改 Hard Spine。

每推进 30～50 章，依据已提交 Story State 重新生成一段滚动章纲，并记录旧计划、新计划、调整原因、软节点变化和保留的 Hard Plot Node。

每卷结束生成 `VolumeSettlement`，只向下一卷提出 Soft Layer 调整建议。终局和 Hard Spine 仍由作者源决定。
