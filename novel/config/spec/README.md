# 作者规格编译层

本目录是 `Outline of Novel Writing/` 的机器可读编译结果，不替代也不修改作者 DOCX。作者 DOCX 是最高规格；编译资产用于校验、检索和生成当前章上下文。

`99_原始资料归档/` 被明确排除，不进入 Writer Context。旧的 `novel/outline/` Phase 3A 资产保留作历史对照，但当它与新版作者 DOCX 冲突时，以本目录及作者 DOCX 为准。
