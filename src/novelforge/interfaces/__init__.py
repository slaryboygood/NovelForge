"""Interface adapters（V4：REST 与 MCP 平级）。

```text
interfaces.rest  → application.services
interfaces.mcp   → application.services
```

两者都只是协议适配层；业务能力全部在 `application.services`。
"""

__all__: list[str] = []
